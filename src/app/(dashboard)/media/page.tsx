"use client";

import MediaManager from "@/components/media/MediaManager";

export default function AllMediaPage() {
  return (
    <MediaManager
      pageTitle="Media Centre Overview"
      pageSubtitle="Consolidated management dashboard for Press Releases, Media Coverage, Newsletters, and Media Connect."
    />
  );
}
