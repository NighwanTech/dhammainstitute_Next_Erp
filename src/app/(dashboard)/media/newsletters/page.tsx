"use client";

import MediaManager from "@/components/media/MediaManager";

export default function NewslettersPage() {
  return (
    <MediaManager
      fixedCategory="newsletters"
      pageTitle="Monthly Newsletters & Research Digests"
      pageSubtitle="Upload quarterly research digests, monthly bulletins, and PDF publications for digital reading & download."
    />
  );
}
