"use client";

import MediaManager from "@/components/media/MediaManager";

export default function MediaCoveragePage() {
  return (
    <MediaManager
      fixedCategory="media-coverage"
      pageTitle="Media Coverage & Clippings"
      pageSubtitle="Manage national and local newspaper features, TV interviews, and digital health news coverage."
    />
  );
}
