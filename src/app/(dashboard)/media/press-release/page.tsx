"use client";

import MediaManager from "@/components/media/MediaManager";

export default function PressReleasePage() {
  return (
    <MediaManager
      fixedCategory="press-release"
      pageTitle="Press Releases"
      pageSubtitle="Publish and manage hospital press releases, clinical milestones, and institutional announcements."
    />
  );
}
